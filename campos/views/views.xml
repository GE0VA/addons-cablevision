<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="campos.list">
      <field name="name">campos list</field>
      <field name="model">campos.campos</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="campos.action_window">
      <field name="name">campos window</field>
      <field name="res_model">campos.campos</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="campos.action_server">
      <field name="name">campos server</field>
      <field name="model_id" ref="model_campos_campos"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="campos" id="campos.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="campos.menu_1" parent="campos.menu_root"/>
    <menuitem name="Menu 2" id="campos.menu_2" parent="campos.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="campos.menu_1_list" parent="campos.menu_1"
              action="campos.action_window"/>
    <menuitem name="Server to list" id="campos" parent="campos.menu_2"
              action="campos.action_server"/>
    -->
  </data>
</odoo>